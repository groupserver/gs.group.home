<div>
  <div tal:condition="view/isMember" id="member-settings">
    <ul>
      <li>
        <a tal:attributes="href string:/groups/leave.html?groupId=${view/groupInfo/id}"
          href="#"><strong>Leave</strong> 
          <span class="group" tal:content="view/groupInfo/name">this group</span></a>
      </li>
      <li>
        <a href="email_settings.html"><strong>Change</strong> your email 
          settings</a> 
        to you recieve one email per post, a daily
        <strong>digest</strong> of topics, or to use this group using
        the <strong>web only.</strong>
      </li>
    </ul>
  </div><!--member-settings-->
  <div tal:condition="view/userInfo/anonymous" id="anon-settings">
    <ul>
      <li>
        <a tal:attributes="href string:/login.html?came_from=${view/groupInfo/url}"
          href="#"><strong>Login</strong> if you are already a 
          member.</a>
      </li>
      <li tal:condition="view/canJoin">
        <a tal:attributes="href string:/login.html?came_from=${view/groupInfo/url}"
          href="#"><strong>Login</strong> if you are already have a 
          profile</a> and you want to join
          <span class="group" tal:content="view/groupInfo/name">this group</span>.
      </li>
      <li tal:condition="view/canJoin">
        <a tal:attributes="href string:/request_registration.html?form.groupId=${view/groupInfo/id}"
          href="#"><strong>Sign up and join</strong></a>
          <span class="group" tal:content="view/groupInfo/name">this group</span>
        if you do not have a profile.
      </li>
    </ul>
  </div><!--anon-settings-->
  <div tal:condition="python:not(view.isMember) and not(view.isAnon) and view.canJoin" 
    id="loggedin-settings">
    <ul>
      <li>
        <a href="join.html"><strong>Join</strong>
          <span class="group" tal:content="view/groupInfo/name">this group</span></a>
      </li>
    </ul>
  </div><!--loggedin-settings-->
</div>

